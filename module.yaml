name: forge-avatar
version: 0.2.0
description: Digital avatar loader. USE WHEN you need identity, preferences, or goals context.
events:
  - SessionStart
metadata:
  name: [name, title]
  description: description

# External steering paths — override in config.yaml (gitignored).
steering: []
  # - /path/to/your/steering/directory/
